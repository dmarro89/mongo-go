apiVersion: v1
kind: Service
metadata:
  name: mongo-go
  labels:
    run: mongo-go
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    run: mongo-go